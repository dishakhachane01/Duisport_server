<odoo>
  <data>
    <record id="crm_case_search_view_leads_inherit_clean" model="ir.ui.view">
      <field name="name">crm.lead.search.view.cleaned</field>
      <field name="model">crm.lead</field>
      <field name="inherit_id" ref="crm.view_crm_case_opportunities_filter"/>
      <field name="arch" type="xml">

        <!-- Remove all <filter> elements -->
        <xpath expr="//filter" position="replace"/>

        <!-- Remove the entire group by block -->
        <!-- <xpath expr="//group" position="replace"/> -->
        
        <!-- Optional: remove <separator/> too -->
        <!-- <xpath expr="//separator" position="replace"/> -->
      </field>
    </record>
  </data>
</odoo>
