
<odoo>
    <record id="view_order_form_revision" model="ir.ui.view">
        <field name="name">sale.order.form.revision</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">

            <xpath expr="//header" position="inside">
                <button name="action_create_revision"
                        type="object"
                        string="Create Revision"
                        class="btn-primary"/>
            </xpath>

            <xpath expr="//sheet//group" position="inside">
                <field name="revision_number" readonly="1"/>
                <field name="original_name" readonly="1"/>
            </xpath>

        </field>
    </record>
</odoo>
