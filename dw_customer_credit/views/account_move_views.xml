<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        
        <record id="view_account_move_form_inherit_credit" model="ir.ui.view">
            <field name="name">account.move.form.inherit.credit</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                
                <field name="partner_id" position="after">
                    <div class="alert alert-danger" role="alert" invisible="not exceeds_credit_limit">
                        <strong>Credit Limit Exceeded:</strong> This invoice exceeds the customer's credit limit.
                    </div>
                    
                    
                    <field name="credit_check_required" invisible="1"/>
                    <field name="exceeds_credit_limit" invisible="1"/>
                    <field name="available_credit" invisible="1"/>
                </field>
            </field>
        </record>
    </data>
</odoo>